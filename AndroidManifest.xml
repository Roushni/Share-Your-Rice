<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shareyourrice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_page_for_donor" />
        <activity android:name=".request_processing_message" />
        <activity android:name=".donors_info_submission_page" />
        <activity android:name=".volunteers_signup_page" />
        <activity android:name=".sign_in_page_volunteers" />
        <activity android:name=".volunteers_Wall" />
        <activity android:name=".notifications" />
        <activity android:name=".get_info"></activity>
    </application>

</manifest>